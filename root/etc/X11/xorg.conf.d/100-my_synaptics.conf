Section "InputClass"
	Identifier "ELAN0662:00 04F3:317C Touchpad" #You could also call that "mycooltouchpadrules" it will only matter if the same identifier is used to change configuration again in another xorg snippet.
	MatchIsTouchpad "on"
        Driver "libinput"
	Option "NaturalScrolling" "true"
EndSection
